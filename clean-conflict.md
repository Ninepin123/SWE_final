# Clean Conflict Demo

A deliberate merge conflict for testing whether @claude can fully resolve
it and push a merge commit. This branch is cut from a main that already
contains the workflow files, so merging main in will NOT touch
`.github/workflows/` — the bot's GITHUB_TOKEN can push the result.

## Feature list

- Feature A: dark mode
- Feature B: export to CSV
- Owner: feature team
- Priority: 100
